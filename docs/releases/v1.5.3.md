# 帧澈 ZENCHE v1.5.3（GitHub 最新稳定版）

> 候选日期：2026-08-04（Asia/Shanghai）
> 版本：`1.5.3 / build 28`
> 分支：`agent/1.5.3-ui`
> 代码候选提交：`846e1a0dc49c59b0cc5d032d84f954a98a61add0`
> 发布提交：`697f3f8d1028426dc5eec430230dcf48754f9b15`（仅含 README/CHANGELOG 发布信息同步）
> 状态：已推送 `main`、已打注释标签 `v1.5.3`，[GitHub Release](https://github.com/Tauber01/ZENCHE/releases/tag/v1.5.3) 已于 2026-08-04T07:43:34Z 发布为 Latest，非草稿、非预发布；未部署生产

## 本轮重点

- 全屏监看采用影像优先的专业 HUD：顶部连接/曝光遥测、焦点十字、左右工具与录制操作、实际 RGB 数据示波器、明确的静音音频基线，以及底部参数托盘。
- 拍摄与参数页采用设备摘要、自适应参数卡和常驻拍摄操作区，让连接方式、输出格式、曝光参数与文件库数量保持可见。
- 编辑器采用媒体池、中央预览、工具检查器和分析示波器协作布局；现有专业显影、AI 工具、蒙版、色轮、曲线和高质量副本保存链路均保留。
- 五端统一深色工作台颜色角色：ZENCHE 蓝用于主操作和选中状态，暖金只用于活动参数读数，红色只用于录制、停止录制和危险操作。

## 数据与兼容性边界

- 新界面不创建第二套业务状态：相机连接、取景、快门、AF、外录、图库与编辑操作继续走 1.5.2 的既有服务和动作。
- RGB 示波器只显示实时画面直方图或本地编辑分析值；没有音频源时显示静音基线，不模拟音量。
- 编辑调整继续写入高质量新副本，不覆盖原文件；设备码恢复、AI 次数、激活码、USB/PTP 与无线传输协议没有改变。
- 参考图只用于信息层级、密度与布局方向，不复制第三方商标、品牌名称或专有图标。

## 审查收敛

- 恢复 iOS 拍摄页的会话配置与开始/结束入口；五端原有拍摄、监看、编辑、AI、传输和设备码恢复入口均保留。
- 所有新增 HUD、设备摘要、底部读数、工具轨和参数托盘都按真实连接状态显示；无可用画面或相机数据时统一降级为 `—`、`OFFLINE` 或无源提示，不显示初始化默认值。
- Android 编辑器的色轮、曲线和蒙版快捷入口会展开并滚动到实际控件，AI 入口进入现有 AI 工作区，不再以提示消息代替操作。
- Windows 编辑工作台增加窄窗断点：窗口宽度低于 1120 时收起媒体栏并压缩工具栏，保留中央预览可用宽度。
- 新增界面标签、空态与辅助功能提示已补齐简体中文、英文、日文资源。

## 当前验证

- `git diff --check` 通过；`test/native-ui-1.5.3.test.mjs` 8/8 通过；完整 `npm test` 256/256 通过。
- iOS / iPadOS：Release、iPhone Simulator、关闭签名构建通过。
- Android：`assembleDebug --rerun-tasks` 31 项任务执行完成，构建通过。
- HarmonyOS：release `assembleHap` 完成真实 ArkTS 编译与 HAP 生成；项目未配置 signingConfigs，因此跳过签名。
- macOS：包含共享相机存储、远程拍摄和全部 macOS Swift 源码的类型检查通过；仅保留既有弃用与 Sendable 警告。
- Windows：`.NET Release` 的 XAML/C# 构建通过，0 错误；仍需真实 Windows 主机安装、驱动与 SmartScreen 验证。
- 当前候选未发现 P0/P1 阻塞；新增三语用户文案已完成一致性与自然度校对。
- 七个版本化交付包均由代码候选提交 `846e1a0dc49c59b0cc5d032d84f954a98a61add0` 生成；同名 `.sha256` 已逐个通过 `shasum -a 256 -c` 回验，容器和平台版本号检查通过。

## 交付物

交付目录：`dist/`

| 交付物 | 字节数 | SHA-256 | 签名/容器状态 |
| --- | ---: | --- | --- |
| `ZENCHE-1.5.3-android.apk` | 3,236,002 | `3c79546bb80ea1d1043aae06fc4d5b848d661b72f871f646b3a4c4db8379b182` | Android Debug 证书；证书 SHA-256 `45499c18366356314c10ad8a98939908e47855fb6ba7fc21fe8a809549c7df3c` |
| `ZENCHE-1.5.3-HarmonyOS.hap` | 1,522,674 | `9dc53cce4375714bfedaac0f7df76e6d225cdeb6fa86f556c6aadc5c30bbd328` | release HAP；项目无 signingConfigs，包内无签名条目 |
| `ZENCHE-1.5.3-ios-unsigned.ipa` | 3,991,340 | `9ddff92fc0cfc8a98d56edef017a475241ae1f8b4911b84cb559ddba0cd215dd` | unsigned IPA；`codesign` 确认 app 未签名 |
| `ZENCHE-1.5.3-macOS-arm64.dmg` | 67,499,588 | `b921f2c3573891fc340e1fac627aa663fa74c7bf28f4ea62a161b8b2eb5e81a5` | Apple Silicon；app 为 ad-hoc，`codesign --verify --deep --strict` 与 `hdiutil verify` 通过；未公证，Gatekeeper 拒绝 |
| `ZENCHE-1.5.3-Windows-x64-Setup.exe` | 90,793,493 | `24769cf08627890ee6d67a23aa0567b92b3b9de5c1e0e08d5485a9ec1f7b631c` | NSIS 安装器；PE Security Directory 为 0，未做 Authenticode |
| `ZENCHE-1.5.3-Windows-x64.zip` | 110,072,800 | `fd9dbcba313d04180b5561e9e3bc96097247c931dc4558305a547f77e10470dc` | 便携包；`unzip -t` 通过，主程序 PE Security Directory 为 0 |
| `ZENCHE-1.5.3-source.zip` | 8,949,449 | `686c318574b78186e8cd80bb41c01130bb18b988ff3c7344fa253a330825e382` | `git archive` 源码包；归档注释为代码候选提交，`unzip -t` 通过 |

每个交付物旁均有同名 `.sha256` 文件。专有 Nikon/Sony SDK 压缩包未进入源码 ZIP 或 Git 工作树；桌面包仅包含运行所需的官方 SDK 文件和说明文档。

## 发布与线上回验

- 2026-08-04 推送 `main` 至 `697f3f8d1028426dc5eec430230dcf48754f9b15`，创建注释标签 `v1.5.3`（标签对象 `64d69abe2f2fce4969cc26a89672255bdc9281ad`，解析到同一发布提交）。
- GitHub Release 共 14 个附件（七个交付包 + 七份同名 `.sha256`）；发布后通过 GitHub API 逐项比对线上字节数与 `digest` 字段，14/14 与本地文件完全一致，零差异。
- Release 正文为详细简体中文，覆盖亮点、平台变化、相机兼容、验证、签名状态、已知限制、升级指引与 SHA-256 表。

源码 ZIP 固定于代码候选提交，因此包内的发布说明与任务进度是打包前快照；包含最终交付表和签名回验结果的版本以当前工作树中的文档及后续文档提交为准。

## 签名与实机限制

- Android 使用 Debug 证书身份；不是商店 release keystore。
- HarmonyOS 与 iOS / iPadOS 为未签名候选，需要对应开发者证书/Profile 重签。
- macOS 为 ad-hoc、未公证候选；不具备 Developer ID 正式更新属性。
- Windows 包可在 macOS 交叉生成，但不能替代真实 Windows 主机上的签名、安装、升级、卸载、驱动和 SmartScreen 验收。
- Windows 安装器资源版本为 `1.5.3`，便携主程序为 `1.5.3.0`；PE 资源未单独编码跨端候选号 `build 28`。
- 相机实机、TalkBack/VoiceOver、HarmonyOS 横屏安全区和长时间监看资源趋势仍需继续扩大验证。
