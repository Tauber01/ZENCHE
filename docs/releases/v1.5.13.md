# 帧澈 ZENCHE 1.5.13 候选发布说明（草稿）

> 候选日期：2026-08-11（Asia/Shanghai）
> 冻结验证日期：2026-08-12（Asia/Shanghai）
> 目标版本：`1.5.13 / build 40`
> 工作分支：`agent/1.5.13-local-downloads`
> 起始基线：`32aeeba0757d2fabbe1eff3c2493c50abe7b7233`
> 冻结实现提交：`b31ed953e1019cbde5fa965c2d0b66efc82f70a3`
> 状态：仅本地候选；未推送、未打标签、未创建 GitHub Release、未切换官网自动更新
> 公开基线：GitHub 公开稳定版仍为 `v1.5.12`；官网自动更新仍提供 `1.5.10 / build 37`

## 候选目标

本候选为 iOS/iPadOS、Android、HarmonyOS、macOS 与 Windows 增加统一的“下载到本地”能力。用户可以把以下 ZENCHE 文件另存到自己选择的位置：

- 联机拍摄和外录产生的照片或视频；
- 从相机存储卡下载到 ZENCHE 文件库的文件；
- AI 修图与 AI 生图结果；
- 专业显影和其他编辑流程生成的新副本。

五端文件库均提供统一入口；支持应用内预览的页面也提供快捷入口，AI 与专业编辑工作区提供直达入口。移动端已有的“保存到系统相册”继续独立保留，不与本地文件保存器合并。

## 数据与错误语义

- 导出只创建副本，不移动、删除或原位修改 ZENCHE 文件库源文件。
- AI 与编辑工作区的直达流程先生成新的文件库副本，再打开系统保存器。macOS 与 Windows 的 AI 修图改为始终创建新文件，不再覆盖当前原图。
- iOS/iPadOS 使用系统文档导出器，Android 使用 Storage Access Framework，HarmonyOS 使用 DocumentViewPicker，macOS 使用 NSSavePanel，Windows 使用 SaveFileDialog。保存位置、文件名与同名覆盖确认由操作系统负责。
- 用户取消是中性结果，不显示成功，也不显示错误。只有完整复制、同步落盘并通过目标大小校验后才报告成功。
- 失败会清理本次临时文件；对于 Android/HarmonyOS 文档提供器目标，会尽力删除不完整目标。由于跨提供器通常不支持事务重命名，不能把移动端文档提供器写入描述为严格原子提交。
- 桌面端在目标同目录写入受限命名的临时文件，完成同步和大小校验后再替换或同卷移动。macOS 使用文件资源标识，Windows 使用卷序列号与文件索引，并在复制前和发布前复核，拒绝符号链接、Finder alias、junction 或 hardlink 指回源文件；大文件复制在后台执行，避免阻塞界面。
- Android 在系统保存器期间保存库内相对路径，Activity 被系统重建后仍可恢复待导出的源文件；写入前以设备号和 inode 拒绝目标指回源文件，完成 `fsync` 后读取目标文件实际大小再报告成功。HarmonyOS 的复制、同步和大小校验通过 TaskPool 执行。
- 导出保持原文件字节和扩展名，不重新编码 JPEG、RAW 或视频。五端文件库统一识别 JPG/JPEG、HEIF/HEIC、PNG、TIF/TIFF、NEF/NRW、ARW、CR2/CR3、MOV/MP4/M4V 与 AVI；能否直接预览仍取决于平台解码能力。

## 相机与兼容性

- 本候选不增加或删除相机档案，也不改变 USB/PTP、PTP/IP、实时监看、拍摄或外录协议。
- 现有 Nikon、Sony、Canon 注册表与各平台能力边界保持不变；相机注册表不等同于全部机型已经完成实机验证。
- “下载到本地”覆盖已经进入 ZENCHE 文件库的受支持 JPEG、RAW 与视频文件。系统保存器、文档提供器、权限、同名覆盖、无空间和大文件行为仍需各平台实测。

## 本地候选产物

以下结果来自冻结实现提交 `b31ed953e1019cbde5fa965c2d0b66efc82f70a3`。它们不是 GitHub Release 资产，也没有进入官网更新通道：

| 平台 | 文件 | 字节数 | SHA-256 | 分发边界 |
| --- | --- | ---: | --- | --- |
| Android | `ZENCHE-1.5.13-android.apk` | 3,320,989 | `3753579bee5858dd07739b36999c84a079b147f2fbd2829afb79c2b04d4bd6ff` | v2 Android Debug 证书，不是正式发布签名 |
| iOS / iPadOS | `ZENCHE-1.5.13-ios-unsigned.ipa` | 4,477,877 | `5e37ffacf07c264dc8a6337ffaca2d99a2d9137165ac6b8636cfa34cd7667bfa` | `CODE_SIGNING_ALLOWED=NO`，未签名，不能直接安装 |
| HarmonyOS | `ZENCHE-1.5.13-HarmonyOS.hap` | 4,469,351 | `608c042dddf97128e86912f8f680475e032dabfc14ee35aac2e6fa02b6d9a246` | 构建日志确认未配置 `signingConfigs`，不可作为正式安装包 |
| macOS | `ZENCHE-1.5.13-macOS-arm64.dmg` | 67,229,657 | `b59afbfdec0360a5a2ef2cadf0834b8ba723d6733d45c7783a60997b33cc7451` | ad-hoc 签名、未公证；未执行 Gatekeeper/真实安装验收 |
| Windows x64 | `ZENCHE-1.5.13-Windows-x64-Setup.exe` | 90,864,688 | `de6bcb5e079e0b90ad6e1c1d4a4e50285da1edc6940f1dd45015a395da18d128` | macOS 主机交叉构建、无 Authenticode；未执行真实 Windows 启动、安装、驱动或 SmartScreen 验收 |
| Windows x64 便携版 | `ZENCHE-1.5.13-Windows-x64.zip` | 110,143,349 | `df13e67ece6d359b039b587eb77b71ddafa5749aca949c65e2b9062c2bf53f3e` | 同上；ZIP 压缩结构与完整运行库清单已通过静态校验 |

六个独立候选包及六份同名 `.sha256` 侧车均来自冻结实现提交，侧车回验 6/6 通过。当前未生成 1.5.13 聚合交付包；不得复用、改名或引用 1.5.12 及更早版本的包和哈希冒充本候选。

## 验证矩阵

| 门禁 | 当前状态 | 完成标准 |
| --- | --- | --- |
| 五端源码与三语 | 实现完成；Harmony 编辑失败提示补齐三语并增加契约；最终只读复核 P0/P1=0 | 五端入口、直达流程、错误状态和简中/英/日文案一致 |
| 自动化测试 | 冻结实现提交 `npm test` 532/532，0 失败 | 通过 |
| iOS / iPadOS | 冻结提交 Release 免签名构建、IPA 结构、`1.5.13 / 40` 元数据通过 | 系统文档导出器、取消、同名、大文件与无空间仍需真机验证 |
| Android | 冻结提交全量 Debug APK、容器、版本及 v2 签名检查通过；证书 SHA-256 为 `45499c18366356314c10ad8a98939908e47855fb6ba7fc21fe8a809549c7df3c` | SAF 文档提供器、生命周期恢复、失败清理与权限边界仍需真机验证 |
| HarmonyOS | 冻结提交全量 HAP、容器和 `1.5.13 / 40` 元数据通过；构建明确跳过签名 | DocumentViewPicker、后台复制、失败清理与权限边界仍需真机验证 |
| macOS | 冻结提交 app/DMG 构建、应用深度严格验签与 `hdiutil verify` 通过；`1.5.13 / 40`、ad-hoc | NSSavePanel、原子提交、公证、Gatekeeper 与真实安装仍需验收 |
| Windows | 冻结提交交叉 publish、NSIS、ZIP 与容器结构通过；主程序 `1.5.13.40`，Setup 与主程序 PE Security Directory 均为 0 | SaveFileDialog、原子提交、Authenticode、安装、驱动与 SmartScreen 仍需真实 Windows 验证 |
| 独立源码审查 | 最终 P0=0、P1=0；Harmony AI 重复后缀已返修并重建 | 两项 P2 不阻断：Harmony 跨卷 inode 碰撞会保守拒绝；Windows 后台准备期间切换编辑上下文可能在完成时回到新副本 |

静态契约、模拟器、交叉编译或未签名容器均不能替代真实文档提供器、文件系统、权限、同名、大文件、磁盘空间不足、相机输出和安装流程验收。

## 升级与发布边界

- 1.5.13 已冻结为本地候选，但正式签名、真实文档提供器与实机安装门禁尚未闭环；用户应继续使用 GitHub 公开稳定版 v1.5.12，官网自动更新用户继续停留在 1.5.10 / build 37。
- 本草稿不是发布授权，也不是下载入口。未经明确授权，不得推送分支、创建标签或 Release、上传附件、替换既有资产或修改官网生产清单。
- 冻结提交、测试、逐平台构建、包字节数、SHA-256、签名/公证状态与实机限制已回填；是否进入发布流程仍须由 Tauber 另行授权。

## 冻结产物的 SHA-256

- Android：`3753579bee5858dd07739b36999c84a079b147f2fbd2829afb79c2b04d4bd6ff`
- iOS / iPadOS：`5e37ffacf07c264dc8a6337ffaca2d99a2d9137165ac6b8636cfa34cd7667bfa`
- HarmonyOS：`608c042dddf97128e86912f8f680475e032dabfc14ee35aac2e6fa02b6d9a246`
- macOS：`b59afbfdec0360a5a2ef2cadf0834b8ba723d6733d45c7783a60997b33cc7451`
- Windows Setup：`de6bcb5e079e0b90ad6e1c1d4a4e50285da1edc6940f1dd45015a395da18d128`
- Windows ZIP：`df13e67ece6d359b039b587eb77b71ddafa5749aca949c65e2b9062c2bf53f3e`

以上六份侧车均在 `dist/` 内执行 `shasum -a 256 -c` 并返回 `OK`。后续仅文档提交不会改变六包的源码基线；如再修改任何实现或打包脚本，必须重新冻结并重建全部候选。
