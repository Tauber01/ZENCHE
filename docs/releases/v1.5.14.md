# 帧澈 ZENCHE 1.5.14 候选说明

> 候选准备：2026-08-15（Asia/Shanghai）
> 版本：`1.5.14 / build 41`
> 状态：本地候选已完成；未推送、未打标签、未创建 Release、未切换官网
> 冻结实现：`6dbdb0d4802328629ffcbf0a97371a92d5862fd1`

## 本次修复

- 五端 PTP/IP 心跳不再重复发送 `OpenSession(0x1002)`，改为 event TCP 上的
  `ProbeRequest/ProbeResponse`（type 13/14）；常驻 reader 同时响应相机主动探测并
  排空 type 8 事件。
- Apple、HarmonyOS 与 Windows 把 command TCP 的请求、完整数据相位和最终响应纳入
  同一事务串行门；Android 保留既有实例同步边界，避免实时取景、参数、文件与快门
  并发时互相读取响应。
- `GetDeviceInfo(0x1001)` 恢复无参数请求，并按 PIMA 15740 的 PTP STR 与 AUINT16
  解析 Manufacturer；四端都校验 OperationResponse 的 transaction ID。
- data-out 改为 `StartData(tx,totalLength) → EndData(tx,data)`；HarmonyOS 同时补回
  `SetDevicePropValue` 的属性码参数。
- Android 增加 `ACCESS_NETWORK_STATE`，HarmonyOS 增加 `GET_NETWORK_INFO`。
  Windows 每轮重连设 12 秒上限，完成厂商、取景与参数恢复后才退出重连态；HarmonyOS
  会先关闭旧双通道，再调度新连接。
- Apple、HarmonyOS、Windows 以 session generation 隔离断开前后的 writer/握手；
  Android 也用连接代际阻止已取消的初连/重连或本机相机回调重新发布会话；UI 线程
  先发布取消并通过无事务锁的 abort 立即打断 socket，完整状态复位再交给相机执行器。
  手动取消不会被在途重连复活，半握手会清理双通道，
  command-only 传输失败也会让后续心跳进入统一重连。data-in 支持未知长度哨兵并
  核验已知长度。

## 本地候选包与 SHA-256

| 平台 | 文件 | 字节数 | SHA-256 |
| --- | --- | ---: | --- |
| Android | `ZENCHE-1.5.14-android.apk` | 3,329,465 | `3ae1592df9d5b2d957d7a928b5fbe78cb99386b7695dbaf02ba24d19b712bab6` |
| iOS / iPadOS | `ZENCHE-1.5.14-ios-unsigned.ipa` | 4,520,711 | `d8e88747b7c71defaf01e11e3edbab2bf72a06020b7c7783ed80f7a888ff4aa4` |
| HarmonyOS | `ZENCHE-1.5.14-HarmonyOS.hap` | 4,492,032 | `449f97822622c6030e2c9bb98118198e40a6d3e040d945086168f0bac14685c9` |
| macOS Apple Silicon | `ZENCHE-1.5.14-macOS-arm64.dmg` | 68,040,957 | `02b1da671547072a10ffce56d2070645e393445f9a52f5ab5a428f0e63220d38` |
| Windows x64 | `ZENCHE-1.5.14-Windows-x64-Setup.exe` | 90,896,728 | `42b509afa6a0090e01b8da312dbd4d41a88782b1c536e72066b362af805c8b3c` |
| Windows x64 便携版 | `ZENCHE-1.5.14-Windows-x64.zip` | 110,151,582 | `82c37425b8b1a32306aabfe707fe5cc0ab87cab7a2805f61bec03d10f52e414b` |

六包及同名 `.sha256` 均从冻结实现提交重建；本表只记录本地候选，不是公开下载资产。

## 验证

- 冻结实现提交的完整 `npm test -- --test-reporter=dot`：573/573 通过；连接专项
  61/61 通过；测试命令同一 shell 首行确认 HEAD 为上述提交。
- Android Gradle、iOS generic/device Release unsigned、HarmonyOS Release
  `assembleHap`、macOS arm64 应用/DMG、Windows x64 Release publish/NSIS/ZIP
  均从上述实现提交完成。
- APK 为 `1.5.14 / 41`，包含 `ACCESS_NETWORK_STATE`；IPA、HAP 与 macOS app 均为
  `1.5.14 / 41`。APK、IPA、HAP 和 Windows ZIP 压缩完整；Windows ZIP 含顶层及
  CrAdapter `libusb-1.0.dll`、Nikon Image/Remote SDK 和 Sony Camera Remote SDK
  运行库，主程序为 PE32+ x86-64。
- NSIS 启动 stub 为 PE32 i386，这是既有跨构建工具输出；安装脚本使用
  `$PROGRAMFILES64` 与 `SetRegView 64`，其载荷和主程序为 x64。Setup 与主程序 PE
  Security Directory 均为 0。
- 六份侧车 6/6 回验 `OK`；macOS `codesign --verify --deep --strict` 和
  `hdiutil verify` 通过。iOS app 没有 `_CodeSignature` 或 embedded profile，
  `codesign` 确认完全未签名；HarmonyOS 包内无签名条目且构建跳过 `SignHap`。

## 签名与未完成边界

- Android 使用 v2 Debug 证书，证书 SHA-256 为
  `45499c18366356314c10ad8a98939908e47855fb6ba7fc21fe8a809549c7df3c`。
- iOS / iPadOS 与 HarmonyOS 未签名；macOS 为 ad-hoc 且未公证；Windows 无
  Authenticode，且由 macOS 主机交叉构建。
- 尚未使用真实 Nikon/Sony/Canon PTP/IP 相机验证主动/被动 Probe、并发实时取景、
  断网和退避重连；也未完成移动端安装、Windows 启动/驱动/SmartScreen 或 macOS
  Gatekeeper 验收。静态契约、编译与容器校验不能替代这些实机门禁。
- 当前每个 PTP/IP 包仍有 64 MiB 防御性上限，超过约 64 MiB 的对象依赖相机拆分为
  多个 Data/EndData 包；单个超大 EndData responder 尚不兼容。5s Probe 周期与 3s
  超时是产品恢复策略，不是 CIPA 规定值，也仍需真机确认。
- GitHub Latest 仍为 1.5.13；官网自动更新仍提供 1.5.10 / build 37。
