# 帧澈 ZENCHE 1.5.14 发布说明

> 发布日期：2026-08-20（Asia/Shanghai）
> 版本：`1.5.14 / build 41`
> 实现封板：`5e8b7f1e81c91d99848c619ba583784f5a20cb57`
> 发布阶段：GitHub Latest 与官网自托管更新源已正式发布，线上资产与公网下载均已回验

## 本次更新

1.5.14 聚焦三件摄影现场最容易打断工作的事：相机连不上或连接中断、电脑里的
照片和视频难以快速进入 ZENCHE，以及文件库层级过深。本版同时补充 Sony ZV-E10
及同代机型的识别与兼容配置，并将五个平台的文件管理统一为“所有文件”优先。

### 相机连接与断线恢复

- 修复五端 Wi‑Fi/PTP‑IP 在心跳、命令、事件和自动重连交错时出现的假断线、响应
  串读与旧会话复活。心跳改为 event 通道标准 Probe Request/Response，常驻 reader
  同时响应相机主动探测并排空事件。
- Apple、HarmonyOS 与 Windows 将完整 command transaction 串行化；Android 保留
  实例同步边界。响应 transaction ID、GetDeviceInfo 数据集以及 data-out 分帧均按
  PTP 约定收紧。
- 连接、重连、取消和断开由会话代际隔离。手动取消后，旧握手、旧 writer 或旧相机
  回调不能再覆盖新状态；command-only 传输失败也会进入同一恢复流程。
- Windows 单轮重连有明确上限，只有在厂商识别、取景、参数与监看恢复后才恢复拍摄
  操作；外部 USB 相机命令使用“有活动即续期”的空闲超时并终止整个进程组，避免
  辅助程序静默挂死。
- 五端顶层均可直接进入 Wi‑Fi/PTP‑IP，失败后可重试，连接中可取消，重连中可停止；
  当前模式、IP、端口和恢复提示与真实状态保持一致。

### Sony ZV-E10 与同代机型

- 五端相机档案从 46 款扩展到 50 款，其中 Sony 从 12 款扩展到 16 款。
- 新增 Sony ZV-E10、A6100、A6400、A6600；同时识别 A6100A 与 A6400A 名称别名。
- iOS 不再把 Sony 误送到 Nikon 兼容桥；macOS 在官方 SDK 明确不支持时可以回退到
  gphoto2 兼容路径。
- 这里的“支持”指机型识别、能力档案和可用传输路径。Sony 当前公开的 Camera Remote
  SDK 支持表未列出上述旧机型，因此完整遥控、实时取景、参数、传输与重连能力仍须
  使用对应真机逐项验证，不能仅凭静态档案视为官方 SDK 全功能支持。

### macOS / Windows 一键导入

- 桌面端新增“导入照片与视频”主操作，可一次选择多个文件，也可取消正在执行的批次。
- 采用流式读写、同目录临时文件、落盘同步、字节数复核与原子发布，避免大文件整块
  占用内存或把半成品暴露给文件库。
- 支持 RAW+JPEG 同目录配对、重复文件跳过、双备份、XMP 与 SHA-256 校验清单。
- 批次逐项报告成功、跳过、失败或取消；某一项失败不会撤销已经安全提交的其他文件。

### 更简单的五端文件库

- iOS/iPadOS、Android、HarmonyOS、macOS 和 Windows 均以“所有文件”为默认首屏，
  提供搜索、照片/视频筛选和最近/名称排序。
- 项目分类与来源/工具默认收起；分类只写入持久化元数据，不移动原文件。
- macOS 使用懒加载网格和 128 项缩略图缓存；Windows 使用回收虚拟化、后台缩略图
  解码和 128 项缓存，降低大型文件库的初次加载与滚动压力。
- 桌面删除改用系统废纸篓/回收站。成功后才清除项目归属，并按文件真实路径同步其
  历史会话备份、校验清单与 XMP；同 stem 的 RAW/JPEG 兄弟仍存在时保留共享 XMP。

## 下载与 SHA-256

最终发布提供六个包和六份同名 `.sha256` 侧车。发布工作流会拒绝缺少侧车、名称
不匹配或摘要不一致的资产。

| 平台 | 文件 | 字节数 | SHA-256 |
| --- | --- | ---: | --- |
| Android | `ZENCHE-1.5.14-android.apk` | 3,346,729 | `e452092c120f8207aa5acb3868a2c9a80aebcc089dadb4a85cc56485878191fe` |
| iOS / iPadOS | `ZENCHE-1.5.14-ios-unsigned.ipa` | 4,613,937 | `ce5f80dbe2d92966946d6b5686033cb93d3e92f586186ba27abd964966b9ea98` |
| HarmonyOS | `ZENCHE-1.5.14-HarmonyOS.hap` | 4,545,876 | `606f1525cfa4366485a43ac1a4342aa38cd35b7527fe79a4a1c0d67db11d0bf9` |
| macOS Apple Silicon | `ZENCHE-1.5.14-macOS-arm64.dmg` | 67,407,512 | `5689a5d9a39c4382497e6c35dd176ebb9bfd03f67de064cd3d7613d02f60cd59` |
| Windows x64 安装版 | `ZENCHE-1.5.14-Windows-x64-Setup.exe` | 90,921,100 | `2beba5e7cbbae310fd235b127fb20e24f01f03f4e6adc97d28d8bf8466764ab8` |
| Windows x64 便携版 | `ZENCHE-1.5.14-Windows-x64.zip` | 110,175,835 | `d7b5d450b4cf0af00488971c343cf729d80b6bce558a718c5a358abd34fb3938` |

## 安装与升级

- 升级前建议先备份正在使用的 ZENCHE 工作目录，完成中的导入或相机传输应先等待结束。
- Android APK 使用项目连续的 v2 Debug 证书；已安装版本只有证书一致时才能原位升级。
- iOS/iPadOS IPA 和 HarmonyOS HAP 未签名，只适用于具备自行签名/安装条件的测试环境。
- macOS DMG 内应用为 Apple Silicon arm64、ad-hoc 签名且未公证；首次运行可能需要按
  系统安全提示手动确认。
- Windows 提供 x64 安装版和便携版，均无 Authenticode；安装版的启动 stub 为既有
  PE32 i386 NSIS 引导器，但实际载荷与主程序为 x64，并写入 64 位安装目录和注册表。
- 如升级后无法连接，请先确认电脑/手机已加入相机 Wi‑Fi，相机已启用 PTP/IP 或电脑
  遥控模式，并核对 IP 与端口；随后使用“重试”，或停止重连后重新建立会话。

## 验证范围

实现封板 `5e8b7f1e81c91d99848c619ba583784f5a20cb57` 已完成以下验证：

- 完整 `npm test -- --test-reporter=tap` 共 630 项：629 通过、0 失败、1 项仅因当前
  不是 Windows 主机而按设计跳过；测试前后 HEAD 和工作树均未变化。
- Sony 档案、连接/会话生命周期、桌面导入、五端文件库、启动公告和版本元数据专项
  142 项中 141 通过、0 失败、1 项 Windows 主机专属回滚用例跳过；Apple 三份
  `.strings` 均通过 `plutil -lint`。
- Android `assembleDebug` 31 项成功；APK 为 `1.5.14 / 41`，包含
  `ACCESS_NETWORK_STATE`，APK Signature Scheme v2 验证通过，证书 SHA-256 为
  `45499c18366356314c10ad8a98939908e47855fb6ba7fc21fe8a809549c7df3c`。
- iOS generic/device Release 构建成功，IPA 为 `1.5.14 / 41`，没有
  `_CodeSignature` 或 `embedded.mobileprovision`，`codesign` 确认未签名。
- HarmonyOS Release `assembleHap` 成功，HAP 为 `1.5.14 / 41`；构建明确跳过
  `SignHap`，容器内没有签名条目。
- macOS arm64 app/DMG 构建成功，应用为 `1.5.14 / 41`；
  `codesign --verify --deep --strict` 与 `hdiutil verify` 通过，签名为 ad-hoc、
  `TeamIdentifier=not set`。
- Windows `.NET 8` Release publish、NSIS Setup 与便携 ZIP 成功。ZIP 共 52 项，包含
  Nikon Image/Remote SDK、Sony Camera Remote SDK 与两处所需的 `libusb-1.0.dll`；
  主程序为 PE32+ x86-64，NSIS 启动 stub 为既有 PE32 i386，二者 PE Security
  Directory 都为 0。
- APK、IPA、HAP 与 Windows ZIP 的压缩结构无错误；六份 `.sha256` 侧车 6/6 通过
  `shasum -a 256 -c`。GitHub 公开资产与官网公网下载的发布后回验结果见下节。

## 发布与官网部署结果

- `main` 与注释标签 `v1.5.14` 已推送；标签对象
  `fea9c2a8774c5b96d409940fb7b55e2f5e907c22` 解引用到发布提交
  `17e87ce6c39aad07f712cd0605efe90899e6e72c`。
- [GitHub Release v1.5.14](https://github.com/Tauber01/ZENCHE/releases/tag/v1.5.14)
  （ID `373311624`）于 `2026-08-19T19:58:12Z` 公开为非草稿、非预发布的
  Latest。[Release workflow run 32295909168](https://github.com/Tauber01/ZENCHE/actions/runs/32295909168)
  在公开前下载全部资产并验证六份侧车，然后原子发布；12/12 资产均为
  `uploaded`，六包的 GitHub 端字节数与 SHA-256 和本表一致。
- 官网生产清单已原子切换到 `1.5.14 / build 41`。`/healthz` 为
  `ok`；`/api/update` 与兼容别名 `/api/updates` 对 Android、iOS/iPadOS、
  HarmonyOS、macOS arm64 与 Windows x64 均返回 1.5.14、正确 URL/SHA-256、
  `update_available=true` 和 `stale=false`。六个公网包均从
  `https://zenche.top/downloads/` 流式下载后计算 SHA-256，与本表 6/6 一致。
- 实际 443 由 1Panel OpenResty 容器提供，生效下载根为
  `/opt/1panel/www/sites/zenche-top/index/downloads/`。旧版资产保留；本次切换的清单与服务备份在
  `/opt/zenche-update-backups/20260819T200402Z-v1514`。

此前连接候选 `6dbdb0d4802328629ffcbf0a97371a92d5862fd1` 与体验候选
`9eaa7c314b7e51f1e6e91d87b284e317d0c3d903` 的安装包均已作废，不会作为 1.5.14
正式资产发布。

## 已知限制

- 尚未使用真实 Nikon、Sony 或 Canon 相机完成主动/被动 Probe、并发实时取景、相机
  参数、断网退避重连、取消/重试和大文件导入的完整验收。
- 尚未完成移动端真实设备安装、Windows 实机启动/驱动/SmartScreen、macOS Gatekeeper
  与可信签名/公证验收。编译、静态契约和容器校验不能替代这些实机门禁。
- 当前单个 PTP/IP 包保留 64 MiB 防御性上限；超过约 64 MiB 的对象依赖相机拆分为
  多个 Data/EndData 包，单一超大 EndData responder 仍不兼容。
- 5 秒 Probe 周期与 3 秒超时是 ZENCHE 的恢复策略，并非 CIPA 规定值；不同相机与
  网络环境的最优阈值仍需实机数据校准。
