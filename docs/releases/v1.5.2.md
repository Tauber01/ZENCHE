# 帧澈 ZENCHE v1.5.2 本地交付候选

> 交付日期：2026-08-04（Asia/Shanghai）  
> 代码提交：`dce831d203b0159c188907ccf15bbe14add33dac`  
> 状态：本地候选；未推送、未打标签、未创建 GitHub Release、未部署生产

## 本轮改动

- iOS / iPadOS、Android、HarmonyOS、macOS、Windows 新增统一全局状态条，显示连接、当前操作和 ZENCHE 文件库总数。
- 五端新增“恢复设备码”入口：网络请求前本地验证旧设备码与旧激活码，成功响应后用当前设备码二次验证新码，再耐久保存。
- Android USB/PTP 仅对已知异步传输故障降级到同步 bulk 传输；首次成功后在当前连接会话复用稳定路径，重连时重置。
- 新增零依赖 AI 代理与回环签发端，覆盖真实 generate→poll→download、资源上限、UTF-8 分块、失败退款、次数耐久写盘、存储 fail-stop、换绑幂等/链式迁移/并发锁/限流/脱敏审计。
- 版本与五端公告统一为 `1.5.2 / build 27`，中英日文案同步。

## 验证

- 精确 HEAD `dce831d`：完整 `npm test` **248/248 通过**，`git diff --check` 通过。
- 独立审查：core `87cb78c` 无 P0；唯一 P1 为迁移链尾退款缺少测试，集成态已补 in-flight 退款和链尾解析覆盖。
- Android：`assembleDebug` 成功；APK 元数据为 `versionName=1.5.2`、`versionCode=27`。
- HarmonyOS：release `assembleHap` 成功；HAP 元数据为 `versionName=1.5.2`、`versionCode=27`。
- iOS / iPadOS：Release iphoneos、arm64、关闭签名构建成功；IPA 内 Info.plist 为 `1.5.2 (27)`。
- macOS：全部 Swift 源码类型检查通过；只有既有 Core Image 弃用、Sendable 和 `onChange` 警告。
- Windows：macOS 上 `.NET Release` 交叉编译通过，0 错误、4 条既有警告；这不是 Windows 主机安装验证。

## 交付物与 SHA-256

| 产物 | 字节数 | SHA-256 | 签名 / 可安装状态 |
| --- | ---: | --- | --- |
| `ZENCHE-1.5.2-android.apk` | 3,229,462 | `d90bc767d0b1b710f66e5a2b8b15a36e41932ab3a6f563f43fdbb6b6c96f87a9` | Android Debug 证书签名，可侧载候选；非正式发布证书 |
| `ZENCHE-1.5.2-HarmonyOS.hap` | 1,489,049 | `da67a6373ad4faaf64e19f512e93206f7218068b6ccd2c68d262dcc77760370f` | release 编译，未配置 signingConfigs；需开发者证书/Profile 重签后安装 |
| `ZENCHE-1.5.2-ios-unsigned.ipa` | 3,894,030 | `97976caca49bd9d00cdfa38f86be0615d7139952e424ffe8f6b3da7ab96712cc` | 无 `_CodeSignature` / provisioning profile；需 Apple 证书与 Profile 重签 |
| `ZENCHE-1.5.2-source.zip` | 8,898,355 | `676d8a2e7bf3b20c3a316cdff035bd9d48ef22d38b4b62a24232f131a5836a25` | 精确归档 `dce831d`，包含五端及 AI 服务源码 |

每个产物旁均提供同名 `.sha256` 文件，并已用 `shasum -a 256 -c` 回验。

## 未生成的平台包

- macOS DMG 未生成：工作区缺少 `S-SDKNEF-001BF-ALLIN.zip`、`S-SDKZ-200BF-ALLIN.zip` 和 `CrSDK_v2.02.00_20260610a_Mac.zip`；钥匙串同时显示 0 个有效代码签名身份。
- Windows Setup.exe / ZIP 未生成：工作区缺少上述 Nikon SDK、`CrSDK_v2.02.00_20260610a_Win64.zip` 与匹配的 `libusb-1.0.dll`，并且当前不是 Windows 主机。正式门禁仍需 Windows 上运行 `scripts/build-windows.ps1` 并验证安装、驱动和 SmartScreen。

## 仍未闭环的门禁

- Android Nikon Z8/Z50/Z50II USB/PTP、TalkBack 与大屏横屏实机。
- HarmonyOS 沉浸横屏底部避让与签名安装实机。
- macOS live-view / 录制资源趋势及签名/公证。
- Windows 真机编译、安装、驱动和 SmartScreen。
- 生产设备码换绑仍默认关闭；DNS/HTTPS、Nginx 精确反代、秘密注入和关闭公网 8787 未完成，当前没有生产改动。

因此本批文件是可审计的本地交付候选，不是 GitHub 稳定版。

