# 帧澈 ZENCHE 1.5.11 本地候选说明

> 候选日期：2026-08-10（Asia/Shanghai）
> 版本：`1.5.11 / build 38`
> 分支：`agent/1.5.10-login-layout`
> 实现与打包源码：`83eb7b22afe0eb90daa2cb99dc0bc675ab03a57e`
> 状态：本地开发验证候选；未部署、未推送、未打标签、未创建 GitHub Release

## 本次变更

- 五端登录页把表单模式改为“已有账号 / 创建账号”，与真正的“登录 / 注册”提交按钮明确区分。
- Apple 登录按钮在请求期间保留动作文字和进度指示，中英日忙碌态使用完整 exact key；切换模式会清除旧错误并把焦点移回邮箱。
- macOS 与 Windows 新增可保存的桌面工作区：恢复主窗口大小和位置，Windows 另恢复最大化状态。
- 主导航、拍摄参数、编辑媒体池、工具面板与底部工具区支持拖动调整；设置页提供默认、拍摄、监看、编辑、紧凑五套预设及恢复默认。
- 保存的窗口离开当前显示器工作区时会被约束回可见区域。
- 五端公告不再把全部包笼统称为“未签名”，改为说明各平台签名状态不同并引导查阅逐包说明；下表分别披露 Debug、unsigned、ad-hoc 或无 Authenticode 状态。

## 交付物

目录：`dist/`

| 文件 | 字节数 | SHA-256 | 状态 |
| --- | ---: | --- | --- |
| `ZENCHE-1.5.11-android.apk` | 3,300,325 | `4d27f0a0bf82a17257ca2dcdda49c646e5d0b0377258896d3a9d55a2a58c9314` | Debug 证书；证书 SHA-256 `45499c18366356314c10ad8a98939908e47855fb6ba7fc21fe8a809549c7df3c` |
| `ZENCHE-1.5.11-ios-unsigned.ipa` | 4,391,531 | `c397aaf2acb64c38a1acea75931aea0d1c847fea202157a61b7f71fe3a888dd2` | 未签名 |
| `ZENCHE-1.5.11-HarmonyOS.hap` | 4,439,703 | `8424800404f56af26b5a518eb70ca12b8a77ece252e6ef5d4eae441c6b5f6e15` | Release 构建，未签名 |
| `ZENCHE-1.5.11-macOS-arm64.dmg` | 67,232,455 | `83aa9b573469c0c123a07dd61d2b35921c19e0c997f88b269173e7a40743fcb7` | arm64，ad-hoc，未公证 |
| `ZENCHE-1.5.11-Windows-x64-Setup.exe` | 90,833,172 | `1c00e15b0b04cf8848901a4ccbf06b4d6582c910585e44127a42d7edbdfb9754` | NSIS，无 Authenticode |
| `ZENCHE-1.5.11-Windows-x64.zip` | 110,131,967 | `b8f62bee61066f5278be5e0dbb34f7b724d44cc864c3982852ced56c54b4a2ff` | x64 便携包，无 Authenticode |

每个文件旁已生成同名 `.sha256`；包含五端安装包、校验文件、发布说明和使用说明的总包将在最终双审闭环后按当前文档重建。

## 验证记录

- 登录/桌面布局专项：53/53 通过。
- macOS 全源 `swiftc -typecheck`：通过；保留既有弃用与并发警告。
- Windows Release `dotnet build -p:EnableWindowsTargeting=true`：通过，0 错误；保留既有 nullable、未使用字段和漏洞源不可达警告。
- XAML 结构校验与首轮 Apple strings lint：通过。
- 完整 `npm test`：492/492 通过；`git diff --check`、XAML 结构校验和三份 Apple strings lint 通过。
- Android：`versionName=1.5.11`、`versionCode=38`；APK v2 签名验证通过，证书与既有 Debug 包一致。
- iOS / iPadOS：`CFBundleShortVersionString=1.5.11`、`CFBundleVersion=38`；应用未签名，IPA 压缩结构通过。
- HarmonyOS：`versionName=1.5.11`、`versionCode=38`；Release HAP 压缩结构通过，构建日志确认未配置签名。
- macOS：`CFBundleShortVersionString=1.5.11`、`CFBundleVersion=38`；arm64 应用深度严格验签通过，签名为 ad-hoc、无 TeamIdentifier；DMG 校验通过。
- Windows：Release publish 与 NSIS 成功；Setup 为 NSIS PE，Setup 与便携主程序的 PE Security Directory 均为 0；便携 ZIP 结构通过。
- 六个包及同名 `.sha256` 全部回验通过；Impeccable 对本轮八个 UI 实现文件检测为 0 条反模式。
- macOS 实窗确认 1.5.11 公告、主工作区和桌面工作区设置卡片能正常渲染；为保护账号信息，终审截图不包含账号字段。AI审查 对 `ed706112…` 给出 P0/P1/P2=0 的视觉/交互 PASS；GPT5.6luna 对同一提交指出两项 P1，Apple exact key 与逐包签名公告已在 `83eb7b2` 修复，返修后最终复核进行中。

## 安装与能力边界

- Android 使用开发环境 Debug 证书；如果设备上旧包证书不同，不能直接覆盖安装。
- iOS / iPadOS IPA 与 HarmonyOS HAP 未签名，需要各平台有效证书和描述文件/Profile。
- macOS 为 ad-hoc 签名且未公证，Gatekeeper 可能阻止首次打开。
- Windows 没有 Authenticode，SmartScreen 可能提示未知发布者。
- 本版本不包含任意浮动面板、跨窗口拖放、跨显示器面板停靠或自定义布局导入/导出。Windows 多显示器/DPI、两端真实拖拽与重启恢复仍需真实桌面环境验收。
- 生产官网更新仍为 1.5.10 / build 37；本地候选不会由自动更新推送给现有用户。

操作方法见 `docs/DESKTOP_WORKSPACE_LAYOUT.md`。
